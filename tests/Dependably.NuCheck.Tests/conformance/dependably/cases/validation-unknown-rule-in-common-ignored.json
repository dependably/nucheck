{
  "name": "validation-unknown-rule-in-common-ignored",
  "description": "A rule id belonging to a sibling tool is legal in common and ignored by this tool.",
  "tool": "$any",
  "files": {
    ".dependably": {
      "common": {
        "rules": {
          "$foreignRule": "error"
        }
      },
      "$tool": {
        "rules": {
          "$rule1": "error"
        }
      }
    }
  },
  "expect": {
    "error": null,
    "resolved": {
      "rules": {
        "$rule1": "error"
      }
    }
  }
}

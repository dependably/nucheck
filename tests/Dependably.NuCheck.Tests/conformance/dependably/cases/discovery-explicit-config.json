{
  "name": "discovery-explicit-config",
  "description": "--config bypasses discovery and accepts any filename.",
  "tool": "$any",
  "files": {
    "ci/config.json": {
      "$tool": {
        "rules": {
          "$rule1": "error"
        }
      }
    }
  },
  "cli": {
    "config": "ci/config.json"
  },
  "expect": {
    "warnings": [],
    "resolved": {
      "rules": {
        "$rule1": "error"
      }
    }
  }
}

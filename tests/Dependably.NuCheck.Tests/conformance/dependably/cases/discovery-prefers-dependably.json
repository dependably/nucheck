{
  "name": "discovery-prefers-dependably",
  "description": "When both files exist in one directory, .dependably wins and .dependably-check is ignored with a warning.",
  "tool": "$any",
  "files": {
    ".dependably": {
      "$tool": {
        "rules": {
          "$rule1": "error"
        }
      }
    },
    ".dependably-check": {
      "$tool": {
        "rules": {
          "$rule1": "off"
        }
      }
    }
  },
  "expect": {
    "selectedFile": ".dependably",
    "warnings": [
      "BOTH_FILES_PRESENT"
    ],
    "resolved": {
      "rules": {
        "$rule1": "error"
      }
    }
  }
}

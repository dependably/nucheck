{
  "name": "validation-bad-severity",
  "description": "An invalid rule severity is INVALID_SEVERITY.",
  "tool": "$any",
  "files": {
    ".dependably": {
      "$tool": {
        "rules": {
          "$rule1": "fatal"
        }
      }
    }
  },
  "expect": {
    "error": "INVALID_SEVERITY"
  }
}

{
  "name": "validation-unknown-rule-own-section-errors",
  "description": "An unknown rule id in the tool own section is UNKNOWN_RULE.",
  "tool": "$any",
  "files": {
    ".dependably": {
      "$tool": {
        "rules": {
          "$unknownRule": "error"
        }
      }
    }
  },
  "expect": {
    "error": "UNKNOWN_RULE"
  }
}
